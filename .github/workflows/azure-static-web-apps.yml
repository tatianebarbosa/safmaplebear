name: Azure Static Web Apps CI/CD

# Static Web App deployment intentionally desativado.
# O frontend oficial deve ser publicado pelo Netlify.
on:
  workflow_dispatch:

jobs:
  disabled_job:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Azure Static Web Apps deployment desativado para evitar conflito com Netlify."
