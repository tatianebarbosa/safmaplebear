# Variáveis de Ambiente para o Projeto MapleBear SAF - Desenvolvimento

# URL base da API do backend (Azure Functions)
# O proxy do vite.config.ts cuida disso, mas é bom ter para builds
VITE_API_BASE_URL=http://localhost:7071

# Chave de API para serviços externos (se houver)
# VITE_EXTERNAL_API_KEY=

# Chave secreta para JWT (JSON Web Token)
# VITE_JWT_SECRET=chave_secreta_para_testes_locais
